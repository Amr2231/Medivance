import { SecurityCenterPage } from "@/features/admin";

// metadata for security page
export const metadata = {
  title: "Security | Medivance",
  description: "Manage security settings for your Medivance account",
};

// Security Page
export default function SecurityPage() {
  return <SecurityCenterPage />;
}
